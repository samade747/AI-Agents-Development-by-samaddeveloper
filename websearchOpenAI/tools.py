def web_search(query):
    return f"Mock result for web search: '{query}'"

def file_search(query, vector_store_ids, max_num_results=3):
    return f"Mock result for file search: query='{query}', vectors={vector_store_ids}"
